(()=>{'use strict';
const INDEX_URL='./content-db/index.json';
const MANIFEST_URL='./content-db/manifest.json';
const SOURCE_VERSION='repository-content-db-v3';
const LOAD_TIMEOUT_MS=15000;
let cache=null;

function withTimeout(promise,ms,label){return Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error(`${label} timed out after ${Math.round(ms/1000)} seconds.`)),ms))])}
async function fetchJson(url){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),LOAD_TIMEOUT_MS);try{const response=await fetch(`${url}?v=${Date.now()}`,{cache:'no-store',signal:controller.signal});if(!response.ok)throw new Error(`${url} returned ${response.status} ${response.statusText}`);return await response.json()}catch(error){if(error?.name==='AbortError')throw new Error(`${url} did not respond within ${Math.round(LOAD_TIMEOUT_MS/1000)} seconds.`);throw error}finally{clearTimeout(timer)}}
function normalize(record,index){return{catalogId:record.databaseId||record.catalogId||`content-${index+1}`,inventoryId:record.provenance?.inventoryId||record.inventoryId||record.databaseId||'',refId:record.stableId||record.refId||'',name:record.name||record.stableId||`Unnamed content record ${index+1}`,contentType:record.objectType||record.contentType||'Unclassified',stage:record.developmentStage||record.stage||'Source identified',source:record.source||record.provenance?.authority||'Multiversal repository content database',sourceLocator:record.sourceLocator||'',tags:Array.isArray(record.tags)?record.tags:[],notes:record.notes||'',coverageStatus:record.coverageStatus||'',promotionDecision:record.promotionDecision||'',reviewStatus:record.reviewStatus||'',expectedOnly:false,databaseSource:'repository',databaseVersion:record.databaseVersion||SOURCE_VERSION,packIds:Array.isArray(record.packIds)?record.packIds:[],dependencies:Array.isArray(record.dependencies)?record.dependencies:[],provenance:record.provenance||{}}}
async function load({force=false,onProgress}={}){if(cache&&!force)return cache;onProgress?.('Loading repository manifest and content index…');let manifest,index;try{[manifest,index]=await withTimeout(Promise.all([fetchJson(MANIFEST_URL),fetchJson(INDEX_URL)]),LOAD_TIMEOUT_MS,'Repository content database')}catch(error){throw new Error(`The generated repository database is not available yet. ${error.message}`)}if(index.format!=='multiversal-content-database')throw new Error('The repository content database has an unsupported format.');if(!Array.isArray(index.records)||index.records.length<1000)throw new Error(`Repository database validation failed: ${index.records?.length||0} records found.`);if(Number(index.recordCount)!==index.records.length)throw new Error(`Repository database count mismatch: manifest says ${index.recordCount}, index contains ${index.records.length}.`);const records=index.records.map(normalize);cache={manifest,index,records,count:records.length,summary:index.summary||{},databaseVersion:index.databaseVersion||manifest?.databaseVersion||'unknown',generatedAt:index.generatedAt||manifest?.generatedAt||null,fallback:false};onProgress?.(`Loaded ${cache.count.toLocaleString()} repository records.`);return cache}
async function getAll(options){return(await load(options)).records}async function count(){return(await load()).count}async function status(){try{const db=await load();return{installed:true,count:db.count,meta:{databaseVersion:db.databaseVersion,generatedAt:db.generatedAt,summary:db.summary}}}catch(error){return{installed:false,count:0,error:String(error.message||error),meta:null}}}function clear(){cache=null;return Promise.resolve()}async function exportDatabase(){return load()}
window.MultiversalContentDB={load,getAll,count,status,clear,exportDatabase,SOURCE_VERSION,INDEX_URL,MANIFEST_URL,LOAD_TIMEOUT_MS};
})();