name: Validate Item Template Registry

on:
  pull_request:
    paths:
      - 'governance/object-system/item-templates/**'
      - 'scripts/validate-item-template-registry.mjs'
      - '.github/workflows/validate-item-template-registry.yml'

jobs:
  validate-item-template-registry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/validate-item-template-registry.mjs
