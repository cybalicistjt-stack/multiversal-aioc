name: Normalize IA-D03-004 Completion Scorecard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: governance checkpoint changes must not regenerate the interaction pilot scorecard."
