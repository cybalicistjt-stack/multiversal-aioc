name: Retired IA-D03-004 Completion Normalizer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Checkpoint changes do not regenerate pilot scorecards."
