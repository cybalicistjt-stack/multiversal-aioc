{
  "schema_version": "1.4.0",
  "status": "CURRENT",
  "profile_id": "MV-EXECUTION-PROFILE-001",
  "authority": "Canonical machine-readable execution service objective and bounded-tranche timing policy.",
  "service_objective": {
    "ordinary_tranche_single_continue_target_percent": 100,
    "max_execution_cycles_without_genuine_blocker": 1,
    "unrelated_historical_validation_jobs_target": 0,
    "reruns_without_changed_evidence_target": 0,
    "post_merge_stale_pointer_target": 0
  },
  "timing": {
    "target_active_minutes_per_unit": 24,
    "minimum_closeout_reserve_minutes": 8,
    "closeout_switch_active_minute": 16
  },
  "execution_envelope": {
    "terminal_gate_required": true,
    "scope": "Every execution-mode owner Continue, including product implementation, governance maintenance, research, evaluation, recovery, and other same-lane executable work.",
    "phases": ["fill", "closeout", "closed"],
    "cycle_identity_rule": "The cycle_id is created once for the owner Continue execution envelope and remains stable across logical operations. Completing a logical operation, research subfacet, tool batch, commit, validation milestone, or artifact does not create a new cycle.",
    "dynamic_fill_rule": "While phase=fill and safe same-lane work exists, completion of one logical operation immediately selects the next safe same-lane operation. The executor may not emit a final response or increment the cycle merely because a logical operation completed.",
    "closeout_transition_rule": "At closeout_switch_active_minute, stop optional expansion and transition the same cycle into shared closeout. The closeout reserve is part of the same owner Continue and cycle, not a new cycle.",
    "terminal_rule": "Normal terminal response requires phase=closed, closeout_complete=true, canonical timing values, and elapsed_active_minutes >= target_active_minutes_per_unit. Early closure is allowed only when fill_exit_reason=safe_work_exhausted, safe_same_lane_work_available=false, and dynamic_fill_attempted=true, or through the separately validated genuine-blocker path.",
    "early_exit_rule": "Safe-work exhaustion must be evidenced after a dynamic-fill attempt. Executor preference, logical-operation completion, a convenient report boundary, or a smaller self-selected target is not exhaustion.",
    "interruption_rule": "If execution is interrupted, persist/resume the same cycle_id and monotonic envelope state. Conversation change, context compaction, or recovery does not reset elapsed work, phase, or cycle identity.",
    "anti_spoof_rule": "The executor may not reduce target_active_minutes_per_unit or closeout_switch_active_minute inside ephemeral termination state. The termination preflight rejects timing values that do not match this profile."
  },
  "execution_reconciliation": {
    "terminal_gate_required": true,
    "reconcile_all_invariants_in_one_pass": true,
    "scope": "Every execution-mode completed_verified terminal attempt after the execution envelope itself is terminal.",
    "desired_state_rule": "The terminal reconciler compares observed execution state to desired_state=terminal_verified. A completed sub-operation or artifact is not desired-state convergence.",
    "operation_identity_rule": "Every logical operation has one durable operation_id and one idempotency_key. Retries increment attempts on the same operation record; duplicate operation or idempotency identities are invalid rather than new progress.",
    "side_effect_rule": "Externally visible effects have durable effect_id/idempotency_key records and must be verified or compensated before terminal response. Applied-but-unverified effects remain nonterminal.",
    "liveness_rule": "Terminal reconciliation requires at least one reconciliation pass and zero consecutive no-progress passes. No-progress is surfaced as an unmet invariant instead of being hidden by narration or repeated reruns.",
    "independent_evidence_rule": "At least one passing verification record must be independent of the executor's completion assertion and bound to the active cycle and trace.",
    "resume_identity_rule": "Interrupted execution increments resume_generation but resumed_from_cycle_id must equal the active cycle_id. Recovery may not silently create a new execution identity.",
    "trace_rule": "Operations, side effects and verification evidence share one trace_id so causal history can be reconstructed across tools, retries, interruption and closeout.",
    "digest_rule": "The terminal reconciliation bundle carries a SHA-256 digest over canonical JSON excluding the digest field. Evidence/state mutation after reconciliation invalidates terminal proof.",
    "one_pass_rule": "The reconciler returns the complete set of unmet terminal invariants in one pass so repairs are batched before another terminal attempt; one-failure-per-Continue discovery is forbidden.",
    "research_basis": [
      "Anthropic long-running agent harnesses externalize progress state instead of relying on model memory",
      "Temporal durable execution replays from persisted history and requires idempotent side-effect handling",
      "Kubernetes controllers repeatedly reconcile observed state toward declared desired state",
      "AWS idempotent retry guidance and transactional outbox patterns separate retry identity from effect identity",
      "SRE readiness and postmortem practice requires evidence-backed operational acceptance",
      "state-machine and fault-injection testing exercise transition sequences and interruption recovery",
      "OpenTelemetry trace-context patterns preserve causal identity across process and tool boundaries"
    ]
  },
  "continue_semantics": {
    "ordinary_bare_continue": "Execute the selected bounded unit through validation, merge, completed_verified closeout, terminal reconciliation and strict successor selection unless a genuine blocker prevents completion.",
    "second_continue_without_genuine_blocker": "execution_incident"
  },
  "execution_hardening": {
    "transaction_preflight": "scripts/execution_transaction_preflight.py",
    "termination_preflight": "scripts/execution_termination_preflight.py",
    "before_product_branch_creation": [
      "validate checkpoint/pointer/authority/runtime/compiled start projection agreement",
      "supply fresh authorized branch plus observed branch/open-PR snapshot",
      "resume the exact authorized branch/open PR when present; never create an alternate duplicate attempt"
    ],
    "validation_evidence_rule": "Only evidence whose validated head equals the current candidate head counts toward merge or closeout; superseded heads are non-counting without needing another broad discovery pass.",
    "closeout_precompute_rule": "During current-head validation, precompute all closeout/successor projections that do not depend on final evidence. Leave validated head, run, deterministic receipt and merge SHA empty until observed.",
    "terminal_outcome_fields": ["owner_continue_turns", "single_continue_achieved", "execution_incident", "genuine_blocker"],
    "terminal_outcome_rule": "For governed execution units completed after MV-CONT-010, owner_continue_turns=1 requires single_continue_achieved=true with execution_incident=null and genuine_blocker=null. More than one owner Continue requires single_continue_achieved=false and exactly one terminal reason: a structured execution_incident when execution stopped without a genuine blocker, or a structured genuine_blocker when an external/environment constraint forced the additional owner turn. The two reasons are mutually exclusive.",
    "final_response_rule": "Execution mode may emit a terminal final response only after a fresh termination preflight returns ALLOW_FINAL_RESPONSE. Open PR, active current-head validation, merged-with-closeout-pending, a nonterminal execution envelope, or unreconciled execution identity/effects/evidence are independently nonterminal even if a pending-step list was accidentally incomplete."
  },
  "context": {
    "repository_first": true,
    "unrelated_history_blocked_by_default": true,
    "refresh_only_on_changed_evidence": true
  },
  "external_tool_access": {
    "required_dependency_status": "verified_current_for_intended_workflow",
    "trial_expired_or_inaccessible_tools_may_be_required": false,
    "known_not_allowed_as_required_development_dependencies": [
      "YepCode",
      "Basic Memory"
    ],
    "fallback_rule": "If a required external development tool is unavailable, expired, trial-limited, or no longer supports the intended AI/development workflow, replace it with a currently verified local/open-source or owner-approved durable alternative before governed start. Do not silently assume access."
  },
  "projection_rule": "Any copied validator/checkpoint/preflight values are generated or checkable projections of this profile and must not become independent authority."
}
