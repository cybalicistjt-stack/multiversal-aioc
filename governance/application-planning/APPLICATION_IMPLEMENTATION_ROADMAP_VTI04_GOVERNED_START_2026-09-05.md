# Application Implementation Roadmap — VTI-04 Governed Start

**Date:** 2026-09-05  
**Work item:** VTI-04 — Rules Action & Roll Bridge  
**Application baseline:** `56ab87c2be214d4d7edb15e0e8d02429a07ee2d4`  
**Registered branch:** `integration/vti-04-rules-action-roll-bridge`

## Governed-start result

VTI-04 is `in_progress` from the exact completed VTI-03 application merge. Branch creation, bounded implementation authority and acceptance-package authority are open. Production mutation remains locked until a genuine matching Linux/Windows RED is sealed.

## Authorized acceptance surface

The VTI-04 acceptance package may require and prove only:

- provider-neutral request envelopes for rolls, attacks, checks, powers, resources, conditions, initiative, reactions and GM adjudication;
- canonical Multiversal actor/source references and stale-request checks;
- deterministic validation/authorization dispositions before Multiversal resolution;
- a Multiversal-owned resolution handoff in which external VTTs never become rules or RNG authorities;
- authoritative result/receipt envelopes and deterministic external-presentation envelopes;
- explicit `present`, `redacted` or `unsupported` presentation fidelity without manufacturing hidden information;
- visibility, ownership, consent, hidden-information filtering and GM-authority preservation;
- completed VTI-03/MIB-03 duplicate/idempotency behavior: authoritative receipt replay, status-before-retry and fail-closed same-idempotency conflicts;
- deterministic normalization, invariant validation and receipts.

## Explicitly blocked

VTI-04 does not authorize provider-specific schemas, vendor selection/ranking, VTI-09 platform commitment, external-VTT rules authority, external-VTT RNG authority, autonomous GM adjudication, credentials, external accounts, adapter implementation, live external synchronization mutation, canonical game-state mutation, durable VTI persistence, a new migration, VTI-05 character-sheet/item/compendium projection, provider activation, tester distribution, release, deployment, VTI-05+ or SGC-01+ implementation.

## Execution contract

1. Validate and merge this governed-start AIOC head.
2. Create the registered application branch from exact main `56ab87c2be214d4d7edb15e0e8d02429a07ee2d4`.
3. Add only the VTI-04 acceptance package and validation profile; do not add production contract code first.
4. Produce genuine matching RED on self-hosted Linux and Windows at the same exact head, with deterministic comparison evidence and zero unrelated historical profile fanout.
5. Seal that RED in governance before production mutation is unlocked.
6. Implement only the bounded VTI-04 production contract necessary to satisfy the accepted RED.
7. Prove exact-head repository health, Linux GREEN, Windows GREEN and deterministic cross-platform comparison.
8. Merge the application PR, terminally retire VTI-04 implementation authority, and select VTI-05 as `selected_not_started` from the exact application merge.

The owner’s `Continue` instruction authorizes this entire bounded execution chain. The tranche must not stop at an intermediate phase unless a genuine external blocker prevents forward progress.
