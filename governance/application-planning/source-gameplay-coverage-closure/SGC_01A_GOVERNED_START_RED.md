# SGC-01A Governed-Start RED

Temporary branch-only evidence marker. The selected checkpoint now carries the owner-approved 100% one-Continue / max-one-cycle service objective. The current repository-health validator still hard-codes the retired 80% / two-cycle objective, so the first exact-head gate is expected to fail on `MVHEALTH-CONVERGENCE-OBJECTIVE` before the shared validator is corrected. Remove this marker before final merge.
