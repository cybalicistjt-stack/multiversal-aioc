# Application Implementation Roadmap — MSWI PDCP Reduction Amendment

**Date:** 2026-09-16  
**Program:** MSWI — Multiversal Systemic Worldplay Integration  
**Status:** owner-approved planning reduction; no implementation authority  
**Historical baseline:** 18 planned tranches  
**Effective planned implementation/proof count:** 7

## Purpose

This amendment records the PDCP family-level reduction of MSWI. It does not rewrite the historical 2026-09-15 MSWI registration amendment; that file remains provenance for the original capability study and 18-tranche plan.

The effective MSWI plan is now governed by:

- `multiversal-systemic-worldplay-integration/MSWI_PROGRAM_BACKLOG.json`;
- `preimplementation-design-closure/PDCP_MSWI_FAMILY_DESIGN_CLOSURE.md`;
- `preimplementation-design-closure/PDCP_MSWI_REDUCTION_RECEIPT.json`;
- `ROADMAP_DEPENDENCY_GRAPH.json` for cross-program activation and golden gates.

## Reduction result

MSWI is reduced from 18 baseline planning tranches to seven surviving implementation/proof tranches:

1. `MSWI-01` — Systemic Transformation State & Integration Core (`01+02`)
2. `MSWI-03` — Cross-System Consequence Routing, Recovery & Domain Adapter Runtime
3. `MSWI-04` — Ecology Succession, Food-Web, Migration & Population-Band Runtime (`04+05`)
4. `MSWI-06` — Composite Entity Role Binding Runtime
5. `MSWI-07` — Biological Components, Harvest, Embodied Modification & Lineage Integration (`07+08`)
6. `MSWI-14` — Systemic Site/Scene Variant, Template & Promotion Integration (`14+15+16`)
7. `MSWI-18` — Systemic Coverage Diagnostics, Golden Proof & SMB-08 Handoff (`17+18`)

Sparse IDs are intentional and preserve historical traceability.

## Cross-family removals

Five baseline MSWI tranches are removed as standalone MSWI runtimes because their generic/canonical authority belongs elsewhere:

- `MSWI-09` doctrine/social runtime → Religion/Culture/Organization + Packet 01 + MRCS/GPR; MSWI retains consequence adapters/proof;
- `MSWI-10` profession/enterprise/downtime runtime → DPL + Project/Time + GPR; MSWI retains consequence adapters/proof;
- `MSWI-11` pursuit/race/chase/convoy/interception runtime → GPR + Movement/Vehicle/Mount/World/Scene/MSLR; MSWI retains consequence adapters/proof;
- `MSWI-12` performance/rhythm/diegetic cue runtime → DPL + MSAS/AAI + GPR + Accessibility; MSWI retains systemic proof only;
- `MSWI-13` grand/world Project framework → APW/D26 Project/Time + GPR + contributing owner domains; MSWI retains milestone consequence routing/variant/proof.

This is deliberate owner consolidation, not capability removal.

## Shared infrastructure absorption

The reduced family also consumes rather than rebuilds:

- PCA-02/PCA-03 for generic procedural recipe/DAG/seed/cache infrastructure;
- PCA-12 + PDCP Packet 08 for generic graph/simulation/formal-analysis infrastructure;
- PDCP Packet 07/canonical owner operations for preview, commit, explanation, intervention and recovery semantics;
- ARI/PCA for generic resource/derivative/rights provenance;
- MCS/MAS/MSAS/MCCS for specialized presentation/authoring outputs;
- Packet 06 and canonical owners for aggregate/cohort/individual resolution semantics.

## DAG reconciliation

No `ROADMAP_DEPENDENCY_GRAPH.json` mutation is required because the milestone IDs referenced by the DAG remain valid:

- `MSWI-01` remains the family start/rotation gate;
- `MSWI-18` remains `MSWI.golden_proof_requires`;
- `MSWI-18` remains an `SMB08.start_requires` capability;
- upstream MSLR golden requirement remains `MSLR-18`.

The reduction changes only the internal MSWI strict order.

## Golden proof preservation

`MSWI-18` now includes the MSWI-specific coverage-diagnostic adapter over PCA-12/Packet-08 plus the family golden proof. The durable DCP defines 36 golden vectors, including absorbed capabilities, so removal of standalone tranches cannot erase acceptance obligations.

The golden battery covers transformation, fan-out/recovery/cycles, ecology, multi-resolution accounting, composite identity, anatomy/harvest, donor-aware modification, social/doctrine integration, profession/life-loop integration, pursuit integration, accessible performance timing, grand Projects, procedural promotion, persistent place variants, transformation templates and systemic coverage diagnostics.

## Effective PDCP count

Historical PDCP baseline: **208**.

After MSLR 18→9 and MSWI 18→7, the effective future count is **188**. Twenty standalone future tranches have been removed so far with complete capability-preserving receipts.

This is not a target count. Remaining family totals change only after their own complete receipts.

## Next reduction pass

`MBES` is the next family selected for PDCP review.

Known high-value overlap clusters to evaluate include:

- `MBES-08+09+10` utility/control/logistics network infrastructure;
- `MBES-12+13` terrain/civil-water transformation;
- `MBES-14+15+16` environment/world response and habitability/resilience;
- `MBES-20+21+23` settlement/regional development and multi-resolution simulation;
- generic diagnostics/simulation absorption into PCA-12/Packet 08;
- generic cross-domain fan-out removal in favor of typed Events/deltas consumed by reduced MSWI.

## OPS3 preservation

This amendment does not modify `operations/CURRENT.json`, start MSWI or SMB-08, grant any implementation branch, reopen MAS, mutate Campaign/world state, authorize paid/cloud dependencies or promote generated/proposed content to canonical truth.
