# Application Implementation Roadmap — MCCS PDCP Reduction Amendment

**Date:** 2026-09-16  
**Project:** PDCP  
**Program:** MCCS — Multiversal Character & Creature Studio  
**Authority:** owner-approved PDCP family reduction  
**Implementation authority:** none

## Decision

The historical MCCS baseline of 21 planned tranches is reduced to **11 effective implementation/proof tranches** after complete intra-family and cross-family overlap review.

Effective order:

`MCCS-01 → MCCS-03 → MCCS-05 → MCCS-06 → MCCS-09 → MCCS-11 → MCCS-13 → MCCS-16 → MCCS-18 → MCCS-19 → MCCS-21`

The historical 21-row baseline remains provenance in `PDCP_MCCS_REDUCTION_RECEIPT.json`.

## Folding summary

- `01+02 → 01` workspace + renderer-neutral projection/draft/rebase authority;
- `03+04+08 → 03` topology + morphology/silhouette + appendage/modular anatomy;
- `05` retained face/sensory/expression anatomy;
- `06+07 → 06` coverings + color/material/pattern/markings;
- `09+10 → 09` wardrobe/equipment fit + accessories/props/linked-being presentation;
- `11+12 → 11` pose/expression + rig/anchor/IK/retarget preview;
- `13+14+15 → 13` shared derivative-renderer/output-profile substrate;
- `16` retained and absorbs only the visual-variation residual of historical `17`;
- historical `17` population/group identity generation is absorbed to MNCS, with generic recipe infrastructure remaining PCA-owned;
- `18` retained as the multi-Form/lifecycle appearance-state adapter;
- `19+20 → 19` appearance interchange/round-trip/cross-renderer/future-P3D handoff;
- `21` retained as golden proof.

## Cross-family ownership

MCCS does not duplicate:

- Character/NPC/Creature or Species/Form entity/body/Form authority;
- CAPP/PPIA renderer-neutral appearance semantics;
- PAPT/PCA generic character-asset, rig, garment, motion, image/sprite and style-production primitives;
- MNCS NPC/creature/group/population identity/composition;
- ARI/PCA generic rights/provenance/version/review/import-export infrastructure;
- Animation/Scene/Combat/Dialogue runtime authority;
- Packet-07 generic preview/recovery/debug semantics;
- deferred P3D physical/3D implementation.

## Atomic dependency-gate reconciliation

Historical `MCCS-02` was a live MNCS start/rotation milestone. Reduced `MCCS-01` absorbs the full renderer-neutral projection/draft-overlay capability previously promised by `MCCS-02`. Therefore this amendment atomically rewrites the equivalent gate:

- `ROADMAP_DEPENDENCY_GRAPH.program_edges.MNCS.start_requires`: `MCCS-02` → `MCCS-01`;
- `ROADMAP_DEPENDENCY_GRAPH.milestone_gates.rotation.MNCS-01`: `MCCS-02` → `MCCS-01`;
- `PARALLEL_PRODUCT_EXECUTION_MAP` MNCS start surface: `MCCS-02` → reduced `MCCS-01`.

This does not weaken or advance MNCS eligibility: reduced `MCCS-01` contains both historical MCCS-01 and MCCS-02 obligations. `MCCS-21` remains MNCS's final appearance/presentation golden-proof dependency.

## Result

MCCS removes **10 standalone future tranches** without detected capability loss. PDCP cumulative effective count becomes **114** from the historical **208**, with **94** standalone future tranches removed across nine resolved families.

The next and final PDCP family review is **MCS**.

`operations/CURRENT.json` is unchanged; this amendment grants no product implementation authority.
