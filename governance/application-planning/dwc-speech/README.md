# DWC Speech Synthesis Project — OPS3 Lane Recovery Entry

**OPS3 LANE REFERENCE / NO GLOBAL OPERATIONAL AUTHORITY**  
**Project:** DWC language-to-speech / native speech synthesis  
**Status:** active preserved DWC lane

## Global authority boundary

Multiversal global operational authority comes only from `operations/BOOTSTRAP.md`, `operations/OPERATING_CONTRACT.md`, `operations/CURRENT.json`, and the current lane/work item selected there. This DWC directory cannot select unrelated application work or redefine global behavior.

## DWC recovery order

When OPS3 selects the `dwc-speech` lane, read:

1. `DWC_PROJECT_ANCHOR.md` for durable DWC goals, architecture, established achievements, constraints, and non-regression boundaries.
2. `DWC_CURRENT_STATE.json` for the lane-local verified execution frontier and next DWC operation.
3. Evidence named by that state, including training, acceptance, external Drive, or live execution evidence.
4. Historical conversation/recovery exports only when detailed provenance is required.

`DWC_CURRENT_STATE.json` is authoritative only inside the selected DWC lane. It never overrides `operations/CURRENT.json` for Multiversal lane selection or global authority.

## Evidence rule

Latest verified DWC artifacts and explicit owner corrections determine the DWC frontier. Missing local files are rehydration problems, not evidence that verified work disappeared. Repeating a proof does not reopen a settled milestone unless contradictory evidence is produced.

Keep this README stable. Update the DWC anchor for durable architecture/invariants and `DWC_CURRENT_STATE.json` when the verified DWC frontier materially advances.
