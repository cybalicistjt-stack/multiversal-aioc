# Application Implementation Roadmap — MSLR PDCP Reduction Amendment

**Date:** 2026-09-16  
**Program:** MSLR — Multiversal Spatial Law Runtime  
**Status:** owner-approved planning reduction; no implementation authority  
**Baseline:** 18 planned tranches  
**Effective planned implementation count after this amendment:** 9

## Purpose

This amendment records the first family-level PDCP roadmap reduction. It does not replace or rewrite the historical 2026-09-15 MSLR registration amendment; that older amendment remains provenance for the original 18-tranche design and placement decision.

The effective implementation plan is now governed by:

- `multiversal-spatial-law-runtime/MSLR_PROGRAM_BACKLOG.json`;
- `preimplementation-design-closure/PDCP_MSLR_FAMILY_DESIGN_CLOSURE.md`;
- `preimplementation-design-closure/PDCP_MSLR_REDUCTION_RECEIPT.json`;
- `ROADMAP_DEPENDENCY_GRAPH.json` for cross-program activation/golden gates.

## Reduction result

MSLR is reduced from 18 baseline planning tranches to 9 surviving implementation/proof tranches:

1. `MSLR-01` — Spatial-Law Profile & Runtime Core (`01+02`)
2. `MSLR-03` — Dynamic Topology State & Governed Spatial Operations (`03+06`)
3. `MSLR-04` — Knowledge, Observer & Spatial Discovery Runtime (`04+05+11`)
4. `MSLR-07` — Reality-Seam, Temporal/Dream & Contextual Spatial Laws (`07+12`)
5. `MSLR-08` — Metric, Scale, Containment & Orientation Transform Runtime (`08+09+10`)
6. `MSLR-13` — Navigation Aids, Anchors, Sensory Cues & Counterplay (`13+15`)
7. `MSLR-14` — Procedural Impossible-Space Authoring, Solvability & Diagnostics (`14+17`)
8. `MSLR-16` — Multi-Resolution Cross-System Spatial Runtime Integration
9. `MSLR-18` — Golden Impossible-Space Proof & MSWI Handoff

Sparse IDs are intentional. They preserve historical traceability and stable roadmap milestone IDs.

## Overlap result

The reduction closes both intra-family and cross-family duplication.

Intra-family overlap clusters are documented in `PDCP_MSLR_REDUCTION_RECEIPT.json`.

Cross-family generic infrastructure is assigned to existing owners rather than repeated inside MSLR:

- PCA-02/PCA-03 — procedural recipe/generator substrate;
- PCA-12 — reusable simulation/formal-analysis workbench;
- PDCP Packet 07/shared owner operations — preview/debug/intervention/reversal semantics;
- MCS — map/cartographic projection;
- GPR/Action/Event — generic gameplay execution, replay and Event mutation;
- MRCS/GPR/Packet 04 — generic affordance/Effect composition;
- MSAS/presentation owners — audio/presentation production;
- MSWI — downstream systemic consequence propagation.

## DAG reconciliation

No `ROADMAP_DEPENDENCY_GRAPH.json` mutation is required for this reduction because the referenced milestone IDs remain valid:

- `MSLR-01` remains the family start/rotation gate;
- `MSLR-18` remains `MSLR.golden_proof_requires`;
- `MSLR-18` remains the MSWI start gate;
- `MBES-24`, `GPR-16` and `MCS-21` remain the current MSLR start requirements until those upstream families receive their own PDCP reduction receipts.

The MSWI compatibility backlog is updated so its upstream wording references the effective PDCP-reduced MSLR strict order rather than the obsolete contiguous phrase `MSLR-01..18 completed_verified`.

## Effective PDCP count

The PDCP historical baseline remains 208 tranches. This first approved family reduction removes 9 standalone future tranches, yielding an interim effective total of **199** while the other nine families still retain their historical baseline counts.

That 199 is not a target or forecast. Further count changes occur only when each family receives a complete capability-preserving reduction receipt.

## Next reduction pass

`MSWI` is the next family selected for PDCP review because it directly consumes MSLR and has strong known overlap in:

- ecology succession / food-web / migration / extinction (`MSWI-04` + `MSWI-05`);
- procedural site remix / persistent map-scene variants (`MSWI-14` + `MSWI-15`);
- shared PCA-12 analysis versus `MSWI-17` diagnostics;
- transformation-state versus consequence-routing boundaries (`MSWI-02` + `MSWI-03`, to be retained separately only if implementation evidence warrants it).

The global folding candidates for all remaining families are recorded in `preimplementation-design-closure/PDCP_CROSS_FAMILY_OVERLAP_REGISTER.md`.

## OPS3 preservation

This amendment does not modify `operations/CURRENT.json`, start MSLR or MSWI, grant an implementation branch, change PCA authority, authorize paid/cloud dependencies, or promote any generated/proposed spatial state to canonical Campaign truth.
