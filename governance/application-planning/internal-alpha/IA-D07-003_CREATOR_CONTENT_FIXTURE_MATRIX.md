# IA-D07-003 Fixture Matrix

Twenty-four deterministic fixtures cover draft creation, proposal revision, rejection, approval, private release, Campaign install, dependency failure, schema failure, forbidden processor, hidden draft filtering, local override pinning, source update divergence, enable/disable, runtime use, invalid-definition block, historical tombstone, import collision, role-filtered export, duplicate request, stale version, lost response, Event-gap repair, screen-reader review, and canonical-promotion refusal.

Each fixture declares versions, authority, request, validation result, authoritative Events, role projections, provenance, and recovery.