# Item Corpus Taxonomy Completion Execution Plan

**Work item:** `ITEM-CORPUS-AUDIT-001`  
**Execution target after audit:** complete the prepared Item corpus projection/adoption safely and measurably.

## Operating rule

Preserve original source CSV bytes. Build derived, reversible, provenance-linked metadata. Use the prepared posture:

**expand → project → validate → review → enable**

No source-unspecified value becomes a guessed value. No name collision becomes an automatic identity merge.

## Tranche 1 — Source and registry recovery

**Goal:** establish all exact inputs needed to make deterministic taxonomy mappings.

Required:
- exact Item preparation v0.12.0 archive, SHA-256 `d12feab1375930e1ef5c121d485aee8622f2abfc34c05f23babb26a293ee47ca`;
- exact 10-axis / 171-value Item registry;
- exact 9-facet / 241-value Content Context registry;
- prepared current-record crosswalks;
- prepared product/creator/origin registries or review queues;
- prepared coverage profile library and validator definitions.

Gate: all recovered package bytes checksum-match canonical manifest. Do not substitute reconstructed values.

## Tranche 2 — Immutable 5,389-row source ledger

**Goal:** one accountable line per governed direct source row.

Already constructed privately as audit baseline:
- 5,389 rows;
- source-file SHA;
- source row number;
- row fingerprint;
- explicit-ID presence;
- field-presence signals;
- Genre source-signal state;
- repeated-name review flags.

Next:
- add exact prepared crosswalk key(s);
- add stable derived working ID where crosswalk rules permit;
- retain source identity independently from canonical/adopted identity.

Gate: dataset counts and exact source hashes equal baseline; no row disappears.

## Tranche 3 — Deterministic shadow taxonomy projection

**Goal:** project the full corpus through v0.12.0 without changing source or current mechanics.

For each row, output:
- taxonomy assertions with stable controlled-value IDs;
- assertion source/evidence class;
- intrinsic vs affinity status;
- setting/content facet candidates;
- unresolved values;
- cross-domain routing;
- supporting-record classification where applicable;
- product/variant review state;
- creator/origin review state;
- compatibility prerequisites only when source-supported.

Status must be exactly one of:
- `projected_deterministic`;
- `projected_review_required`;
- `cross_domain_routed`;
- `supporting_record`;
- `reference_only`;
- `identity_conflict_or_variant_review`;
- `unresolved_source`;
- `not_applicable`.

Gate: 5,389 / 5,389 rows have an accounting status; no fabricated taxonomy values.

## Tranche 4 — Identity, duplicate and variant review

Prioritize the 119 rows participating in 55 repeated normalized names.

Rules:
- same name is never proof of same identity;
- source ID and explicit source relationships outrank names;
- Taser contexts remain separate/conflicted until governed resolution;
- ammo-reference-only names cannot become full weapon Definitions from references alone;
- supporting records inside mixed catalogs are not automatically promoted;
- configuration/variant/product relationships require evidence.

Gate: every repeated-name group has a recorded disposition or explicit unresolved state.

## Tranche 5 — Product / creator / origin projection

Apply the prepared product ladder:
generic concept → family/line → platform/model/product → variant → configuration.

Apply Creator/Origin only where evidence supports it. Generic objects may legitimately have no creator/manufacturer.

Gate: zero name-based product merges; zero invented creators.

## Tranche 6 — Compatibility and context projection

Keep three concepts separate:
- intrinsic requirement;
- affinity;
- evaluated compatibility.

Existing Genre values are evidence/affinity candidates only:
- 63 `All Genres` signals are not a taxonomy genre;
- 5,060 specific Genre values are not automatically hard compatibility;
- 266 blank values are not incompatibility.

Do not issue final compatibility without a governed context/profile and sufficient intrinsic requirements.

Gate: unknown remains valid; no tag explosion.

## Tranche 7 — Coverage and gap analysis

Evaluate the projected catalog across:
- 35 functional domains;
- 28 composable profiles;
- 980 qualitative expectations.

Coverage is diagnostic, not quota-driven. Non-item satisfaction remains allowed.

Gate: every apparent gap distinguishes:
- no evidence;
- non-item satisfaction;
- intentionally unsupported;
- content candidate;
- unresolved.

## Tranche 8 — 54 missing Armor/Materials concepts

Process the prepared future-build seeds under current rules.

For each:
- recover exact source concept and provenance;
- verify it is actually absent rather than an alias/variant;
- build current mechanics through the owning domain rules;
- route Economy/balance dependencies;
- validate against current Item taxonomy;
- publish only through governed content gates.

Gate: no direct numeric conversion from legacy/source mechanics.

## Tranche 9 — Full 5,443-item enrichment queue

Process:
- 5,389 current-record audits/enrichments;
- 54 missing current-rule builds.

Preserve current mechanics by default. Re-mechanize only where a governed defect/gap requires it.

Gate: each work item has a terminal state and provenance.

## Tranche 10 — Governed adoption and A8 linking

Adopt reviewed taxonomy/product/creator metadata in bounded batches, then connect governed Item Definitions to A8 definition snapshots.

A8 remains sole owner of live Asset Instance:
- ownership/custody/possession/control/access;
- quantity/lot/stack;
- location/containment;
- equipment/installation;
- condition/damage/repair;
- consumption;
- runtime transformation/history.

Gate: no duplicate runtime item model and all save/load/offline/import/export regressions pass.

## Completion measure

The corpus is taxonomy-complete only when:

- all 5,389 original source rows remain accounted;
- every row has a terminal projection/adoption/routing status;
- every adopted assertion is tied to exact controlled IDs and evidence;
- every unresolved/unknown remains explicit;
- every repeated-name group is reviewed or explicitly unresolved;
- all 54 source-only missing concepts have governed terminal dispositions;
- all 5,443 enrichment work items have terminal states;
- source hashes and row counts remain unchanged;
- current mechanics are unchanged except for separately governed revisions;
- A8 instance/runtime authority is not duplicated.
