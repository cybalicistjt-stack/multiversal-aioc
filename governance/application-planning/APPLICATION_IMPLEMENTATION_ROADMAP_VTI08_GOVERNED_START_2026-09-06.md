# Application Implementation Roadmap — VTI-08 Governed Start

**Work item:** VTI-08 — Adapter SDK, Capability Manifest & Deterministic Reference VTT  
**State of this commit:** acceptance-only governed-start contract; production mutation remains locked pending genuine matching application RED.  
**Application baseline:** `692da4f4792426b9c62f6be14db60fc63eb09d6b`  
**Implementation branch after this AIOC governed start validates and merges:** `integration/vti-08-adapter-sdk-capability-manifest-reference-vtt`

## Bounded objective

VTI-08 defines a provider-neutral adapter SDK, explicit capability manifest and deterministic fake/reference VTT that exercise the already-completed VTI projection, identity, action/receipt and permission-preservation contracts end-to-end without depending on a commercial platform. Multiversal remains canonical rules, campaign, spatial, identity, permission and adjudication authority.

## Acceptance-only authority opened by this governed start

- create the registered VTI-08 application branch from the exact application baseline only after this AIOC governed start merges;
- add the bounded VTI-08 acceptance profile, fixtures, invariant verifier and tests;
- specify provider-neutral adapter SDK interfaces over completed VTI contracts rather than provider schemas;
- specify deterministic capability-manifest semantics with explicit supported, unsupported, conditional and unknown fidelity;
- specify a deterministic fake/reference VTT as a local derivative test client with no credentials, external accounts or commercial provider dependency;
- prove expected absence of the VTI-08 production contract by genuine matching Linux and Windows RED before production mutation authority opens.

## Production authority remains locked until matching RED

The following remain unauthorized before matching RED is sealed:
- production adapter-SDK, capability-manifest or deterministic reference-VTT implementation;
- commercial provider selection/ranking, provider-specific schemas or VTI-09 first-platform integration;
- provider credentials, external accounts, provider network access or live external synchronization mutation;
- new canonical rules, campaign, spatial, identity, permission or adjudication authority inside the adapter/reference VTT;
- durable VTI persistence or a new migration;
- provider activation, tester distribution, release or deployment;
- VTI-09+ and SGC-01+ implementation.

## Canonical-authority reuse requirement

VTI-08 must consume the existing completed VTI contracts. Its reference VTT is a deterministic test double and derivative presentation/request client only. Capability gaps may reduce fidelity or mark behavior unsupported; they may never broaden disclosure, rules authority, GM authority or canonical mutation authority.

## Exact next action after this governed start merges

Create `integration/vti-08-adapter-sdk-capability-manifest-reference-vtt` from exact application main `692da4f4792426b9c62f6be14db60fc63eb09d6b`, add acceptance-only VTI-08 validation, and obtain genuine matching self-hosted Linux/Windows RED before any production adapter/reference implementation is authorized.
