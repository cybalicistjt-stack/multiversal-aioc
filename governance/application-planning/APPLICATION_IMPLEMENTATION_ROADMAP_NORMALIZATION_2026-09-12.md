# Application Roadmap Dependency Normalization — 2026-09-12

**Status:** CURRENT planning projection after MV-CONT-009 closeout  
**Machine authority:** `ROADMAP_DEPENDENCY_GRAPH.json`

Historical roadmap amendments remain provenance. Cross-program execution now uses typed dependency edges rather than treating every prior `activation_after` or `successor` sentence as a whole-program start blocker.

CNI-01/02 establish pack/dependency foundations. PCA can then overlap CNI on tranche-specific gates; PCA-16 is convergence/golden proof rather than a blanket start blocker for all specialist lanes.

After the common barrier and lane-specific gates, MCS, MCCS, MRCS and MSAS may run simultaneously. MNCS may start at stable MCCS projection capability; MAS at stable CNI runtime; GPR at stable MRCS executable definitions; MERA at stable GPR/MRCS engineering capability; MBES preserves causal MERA dependence. Waiting only for golden integration may surrender mutation authority and free a slot.

Core Player/GM/product UX is SMB10A. SAA follows that seam; SMB10B closes final Creator/SAA integration. A minimum authorization/privacy/family-safety gate precedes remote non-developer alpha. Optional live AI (SMB-12) is not a hard/start prerequisite for SMB-13. SMB-14 remains full hardening. SMB-16 → BRP-01..11 → SMB-17 → SMB-18 remains the release path.

Readiness artifacts and reserved branch names never grant implementation authority.
