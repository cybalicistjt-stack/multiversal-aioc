# Current Game Object Normalization Authority

**Authority ID:** `MV-GAME-OBJECT-NORMALIZATION-AUTHORITY-2026-09-17`  
**Status:** OWNER-AUTHORIZED CURRENT NORMALIZED GAME-OBJECT LAYER  
**Source authority:** `governance/object-system/CURRENT_GAME_OBJECT_SOURCE_AUTHORITY.json`

## What this selects

This file selects the current **normalized working layer** for game-object content work.

The source authority still selects the current source catalogs. This normalization authority sits immediately above it and selects the normalized game-object records derived from those catalogs.

Current normalized accounting:

- **23,560** source rows accounted;
- **21,495** `GAME_OBJECT` records;
- **65** `SUPPORT_RULE` records retained as rules/relationship evidence;
- **2,000** cross-domain records routed to service/organization/education/event/knowledge owners;
- **26,795** inline derivation/construction repairs;
- **0** remaining family content-baseline blockers.

The persistent package is stored in:

`/Multiversal Canonical Game Objects/Normalized v1.0.0`

Primary package:

`CURRENT_FULL_GAME_OBJECT_NORMALIZATION_2026-09-17_v1.0.0.zip`

SHA-256:

`9b8f8fa8419eb262da2e65411686532128ccdd1fdfd2c36bb21ecf6f933d38d3`

## Working rule

For Item/Object/vehicle/mecha/spacecraft/computer/facility/hazard/book/content work:

1. use `CURRENT_GAME_OBJECT_SOURCE_AUTHORITY.json` to select current sources;
2. use `CURRENT_GAME_OBJECT_NORMALIZATION_AUTHORITY.json` to select the current normalized object layer;
3. use the Library index and package referenced there for row/object work;
4. treat earlier P1/CCTI/OGR/FIAA population totals as historical evidence unless explicitly used for lineage/provenance comparison.

Do not fall back to 5,389, 5,443, 10,834, 19,199, 327 melee, or 230 ranged as the active normalized population.

## What normalization means

Every game object now has a common envelope with:

- stable normalized identity;
- domain/type/subtype and runtime projection;
- context, description and discovery metadata;
- physical, economy and operation sections;
- capability modules;
- runtime operation/owner bindings;
- preserved family-specific mechanics;
- exact source-row provenance and source hash;
- explicit source field states;
- verbatim source payload;
- inline repair history.

Missing values were derived or current-rule constructed when reasonably supported by source context, peer/current-rule structure, or the owner's explicit instruction. Every such repair remains labeled by evidence state.

## Important boundary

`CONTENT_BASELINE_COMPLETE` is **not** `GAME_READY`.

This normalization does not claim that every runtime operation is implemented, integration-certified, migration-tested, authorization-tested, or playtested. Those remain the next family-deep certification stages.

## Next stage

Run family-deep mechanics/runtime certification over the 21,495 normalized game objects, preserving this normalized package as the immutable provenance-bearing content baseline.
