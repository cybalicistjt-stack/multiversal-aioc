# Multiversal canonical content sources

This directory is ingested by `scripts/build-content-database.mjs` during GitHub Pages deployment.

Expected canonical source bundle: `phase-1-8-canonical-objects.json`.

Source status: the prior browser upload did not create a committed repository file. The canonical source is being transferred and validated through repository commits instead.
